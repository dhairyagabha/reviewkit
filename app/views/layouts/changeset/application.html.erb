<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Changeset</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
  </head>

  <body class="min-h-screen bg-slate-50 text-slate-950 antialiased">
    <%= render "changeset/shared/flash" %>
    <%= yield %>
  </body>
</html>
