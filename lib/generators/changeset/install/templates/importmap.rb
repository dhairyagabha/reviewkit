# Changeset relies on importmap-rails for its Rails-only Hotwire entrypoint.
# Engine-owned pins are registered automatically; this file exists so Rails can
# build the host application's import map.
